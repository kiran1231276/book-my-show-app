/*
package com.example.ShowTimeService.client;

import com.example.CinemaHallService.dto.CinemaHallDto;
import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;

@FeignClient(name = "cinema-hall-service") // Provide the name of the CinemaHall microservice
public interface CinemaHallClient {

    @GetMapping("/cinema-halls/{hallId}")
    CinemaHallDto getCinemaHallById(@PathVariable("hallId") String hallId);

    // Define other methods as needed
}
*/
